{% comment %}Inkcora — sections/main-page.liquid{% endcomment %}
<section class="section">
  <div class="page-width" style="max-width:800px">
    <h1 class="h1" style="margin-bottom:2rem">{{ page.title }}</h1>
    <div style="font-size:1rem;line-height:1.85;color:var(--color-text)">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Página principal", "tag": "section", "settings": [] }
{% endschema %}
